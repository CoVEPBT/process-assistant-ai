Tracker #36 #39 #40 #41: four small fixes bundled

#40 - Rename Ask PAT to Chat with PAT
The floating chat coach bubble label now reads "Chat with PAT"
instead of "Ask PAT". Single user-facing label change; the
4 other "Ask PAT" references in the file are JS comments only
and were left alone.

#41 - Drop the "AI may have dropped detail" toast
With the activity-structure restore (70271ee), duplicate-NOTE
dedupe (26c65e9), and trigger / trailing-requirement
detection (0aebe8c) all catching the real issues before they
reach the user, the soft-warning toast is now mostly noise.
Removed the showToast call inside the auto-apply drop-
detector. Kept the console.log so engineering can still see
detected drift in DevTools, just doesn't surface to users.

#39 - Promote 'Add activity' button to primary style
The "+ Add activity" button at the end of the doc preview is
now solid RMIT-red on white, larger padding, centred at the
bottom of the activity list. "+ Add task" / "+ Add note"
inside each activity stay discreet (they're contextual and
shouldn't fight for attention with the doc content).
Selector targets .doc-add-btn.doc-add-activity specifically
so only the end-of-list button gets the prominent treatment.

#36 - Link CoVE Process Review Checklist in exports
Both the Word and PDF exports now end with a "Before you
publish" callout linking to the SharePoint-hosted checklist.
Word uses ExternalHyperlink (added to the docx dest