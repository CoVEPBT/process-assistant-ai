Tracker #38 + #42 + #43 + #51

- #38 Parser now reconstructs PROCESS link titles that wrap
  across lines (e.g. "CoVE - SL - A&T Admissions & Enrolment").
  Added currentLink tracking with continuation-line handling,
  mirroring the existing currentTask / currentNote pattern.

- #42 Discreet [x] on user-added tasks / notes / activities so
  the user can undo their own "+ Add" without confirmation.
  Only renders on items the user added - source content is
  untouchable.

- #43 Reset is now context-aware. On the analysed page it
  reverts doc edits + dismissals and stays on the page; on the
  home view it does the original full reset.

- #51 Header Reset label switches between "Start over" (home)
  and "Revert edits" (analysed) via a MutationObserver on
  #resultsCard. Added "Clear all" text button to the paste
  section that wipes title + textarea only.

Tracker: Done 34 -> 38, Pending 17 -> 13. node --check passes.
