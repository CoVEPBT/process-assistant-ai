{
  "name": "process-assistant-ai",
  "version": "0.1.0",
  "description": "RMIT CoVE Process Writing Assistant with inline AI (gpt-4o-mini). Mirrors the comms-engine architecture: Express backend + .env API key + static frontend.",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "dotenv": "^16.4.7",
    "openai": "^4.79.4",
    "mammoth": "^1.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
